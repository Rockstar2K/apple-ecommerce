{{!-- <h1 class="text-center">WearCoder</h1> --}}
<div class="d-flex justify-content-center gap-3 ">

  <label for="filterByCategory" class=" text-black ">Filter by category: </label>
  <select id="filterByCategory" name="category" class="w-50 form-select form-select mb-3">
            <option value="">All</option>
            <option value="iPhone">iPhone</option>
            <option value="iPad">iPad</option>
            <option value="MacBook">MacBook</option>
            <option value="Apple Watch">Apple Watch</option>
            <option value="AirPods">AirPods</option>
            <option value="Apple TV">Apple TV</option>
            <option value="Accessories">Accessories</option>
            <option value="other">Other</option>
          </select>
</div>

<section class="container text-center">
  <div class="row" id="grid-container">
    {{#each data}}
    <div class="col-md-4 mb-4">
      <div class="card mx-auto" style="width: 18rem;">
        <ul class="list-group list-group-flush">
          <li class="list-group-item">
            <a class="product" href="/products/{{this.id}}">{{this.title}}</a>
          </li>
        </ul>
      </div>
    </div>
    {{/each}}
  </div>
</section>

<!--########-->
<section class="container text-center">
  <div class="row" id="grid-container">
    {{#each data.docs}}
    <div class="col-md-4 mb-4">
      <div class="card mx-auto" style="width: 18rem;">
        <ul class="list-group list-group-flush">
          <li class="list-group-item">
            <a class="product" href="/products/{{this._id}}">{{this.title}}</a>
          </li>
        </ul>
      </div>
    </div>
    {{/each}}
  </div>
  <div>
<div class="d-flex gap-2 justify-content-center">
  {{#if data.hasPrevPage}}
    <a href="?page={{data.prevPage}}&limit=6" class="btn btn-primary">Anterior</a>
  {{/if}}

  <p>Página {{data.page}}/{{data.totalPages}}</p>
  
  {{#if data.hasNextPage}}
    <a href="?page={{data.nextPage}}&limit=6" class="btn btn-primary">Siguiente</a>
  {{/if}}
</div>
  </div>
</section>
<!--########-->


<script src="https://cdn.socket.io/4.7.5/socket.io.min.js"></script>
<script type="module" src="/public/js/allProductsSocket.js"></script>